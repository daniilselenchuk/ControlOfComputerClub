<UserControl x:Class="ControlOfComputerClub.View.Controls.QueryControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Запросы по данным" Margin="8" Padding="8">
            <StackPanel Orientation="Horizontal">
                <RadioButton Content="Сотрудники"
                             IsChecked="{Binding IsEmployeesSelected, Mode=TwoWay}"
                             Margin="10 0"
                             GroupName="QueryGroup"/>
                <RadioButton Content="Рабочие места"
                             IsChecked="{Binding IsWorkplacesSelected, Mode=TwoWay}"
                             Margin="10 0"
                             GroupName="QueryGroup"/>
                <RadioButton Content="Заявки"
                             IsChecked="{Binding IsBookingsSelected, Mode=TwoWay}"
                             Margin="10 0"
                             GroupName="QueryGroup"/>
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding QueryResults}"
                  Margin="8"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"/>
    </Grid>
</UserControl>